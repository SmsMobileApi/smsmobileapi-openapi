# Contributing

Open an issue or pull request when the specification differs from documented production behavior.

Keep changes focused, preserve OpenAPI 3.1 compatibility and run:

```bash
npx @redocly/cli lint openapi.yaml
```

Use sanitized examples only. Never add credentials or customer data.
